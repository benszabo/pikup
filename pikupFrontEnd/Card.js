import * as React from 'react';
import {
  Select,
  VStack,
  Stack,
  Container,
  ScrollView,
  Divider,
  Heading,
  Input,
  NativeBaseProvider,
  Center,
  Box,
  CheckIcon,
} from 'native-base';

const Card = () => {
    return (  );
}
 
export default ;