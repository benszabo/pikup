import { DEV_BACKEND_URL, LOCAL_BACKEND_URL, LOCAL } from "@env";

const devEnvVars = { DEV_BACKEND_URL };
const localEnvVars = { LOCAL_BACKEND_URL };
const local = { LOCAL };

export default devEnvVars;