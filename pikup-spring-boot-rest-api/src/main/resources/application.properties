## Database Properties
spring.datasource.url = jdbc:mysql://localhost:3306/pikup_database?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username = root
spring.datasource.password = password!

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

# had to enable less secure app access in gmail front end
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=pikuplfg@gmail.com
spring.mail.password=Pikup2022!
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
